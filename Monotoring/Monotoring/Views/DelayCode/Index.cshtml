@model IEnumerable<Monotoring.Models.DelayCode>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DelayName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DelayName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DelayCodeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.DelayCodeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DelayCodeId })
        </td>
    </tr>
}

</table>
